import { Router } from 'express';
import indexCtrl from '../controller/IndexController';

const router = Router();

//router.post('/upload', indexCtrl.UploadDownloadCtrl.UploadMultipart)

export default router;